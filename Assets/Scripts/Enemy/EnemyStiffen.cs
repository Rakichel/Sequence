using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using PlayerInfo;

public class EnemyStiffen : MonoBehaviour
{
    /*private Player _player;
    private EnemyEnum _enemyEnum;
    // Start is called before the first frame update
    private void Awake()
    {
        _enemyEnum = gameObject.GetComponent<EnemyEnum>();
    }
    private void Update()
    {
        if(_enemyEnum._enemyState == EnemyEnum.EnemyState.Stiffen)
        {
            Debug.Log(_enemyEnum._enemyState);
        }
    }*/
}
