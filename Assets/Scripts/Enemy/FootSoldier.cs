using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FootSoldier : Creature
{
        public FootSoldier()
        {
            //Type = EnemyType.Enemy; // player CreatureType
            //Attack = AttackType.Physical;
            //CurrentState = State.Idle;
        }

        public void kamikaze()
        {
            //if (CurrentState == State.Idle) // walk jump attack hit die
            //{
            //    Debug.Log("Orc goes into a berserk rage!");
            //    Attack();
            //}
        }
    }
}
